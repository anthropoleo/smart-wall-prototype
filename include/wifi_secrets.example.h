#pragma once

// Copy this file to include/wifi_secrets.h and set local credentials.
// include/wifi_secrets.h is gitignored so credentials stay out of the repo ;)
#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"
